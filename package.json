{
  "engines": {
    "node": "16.x",
    "npm": "7.x"
  },
  "scripts": {
    "heroku-prebuild": "cd ./client && npm install && cd ../server  && npm install",
    "start": "cd server && start "
  }
}
